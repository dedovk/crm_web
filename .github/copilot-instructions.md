# Repository Instructions

- After completing any code generation or code change, run `npm run build` and check the output for build errors before finishing.
- If the build fails, fix the relevant errors and run `npm run build` again before reporting the work as complete.
